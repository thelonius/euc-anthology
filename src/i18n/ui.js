// UI dictionary. Keys are Russian source strings (gettext-style).
// Missing key falls back to the key itself (i.e. Russian).
// Filled out in Phase 1 of the i18n migration.

export const UI = {
  en: {
    // Filled in Phase 1.
  },
}
